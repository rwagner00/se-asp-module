<?php 

/*
 * Implements hook_menu
 */

function asp-module_menu {
  $items['tempname'] = array(
    'title' => 'ASP Admin Page' ,
    'page callback' => 'tempname_page',
    'access_arguments' => array('administer content')
  );
  return $items;
}

function tempname_page() {
	
	return $content;
}